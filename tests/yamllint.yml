extends: default

rules:
  line-length:
    level: error
    max: 100
  document-start:
    present: false
    level: error
  comments:
    require-starting-space: false

ignore: |
  ocp_install_default_config.yaml
